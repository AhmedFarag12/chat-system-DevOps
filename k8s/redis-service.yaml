apiVersion: v1
kind: Service
metadata:
  name: chat-system-redis
  namespace: chat-system
  labels:
    app: chat-system-redis
spec:
  selector:
    app: chat-system-redis
  ports:
    - port: 6379
      targetPort: 6379
